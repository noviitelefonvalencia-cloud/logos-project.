name: build-apex
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup-Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: OS-Packages
        run: |
          sudo apt-get update || true
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y build-essential git python3-dev libssl-dev libffi-dev libncurses5 libncursesw5 pkg-config cmake ant unzip

      - name: Python-Packages
        run: |
          python -m pip install --upgrade pip
          pip install "cython<3.0" buildozer virtualenv requests cryptography

      - name: Synthesis-Argus
        run: |
          chmod +x argus.sh
          ./argus.sh

      - name: Buildozer-Android
        run: |
          yes | buildozer android debug

      - name: Save-Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Apex-Package
          path: bin/*.apk
